spring:
  application:
    name: ms-customer

  h2:
    console:
      enabled: true

server:
  port: 6945
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://localhost:8762/eureka
