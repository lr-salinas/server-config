spring:
  application:
    name: ms-sale
  h2:
    console:
      enabled: true
server:
  port: 8743
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://localhost:8762/eureka
